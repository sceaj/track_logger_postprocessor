'''
Created on May 20, 2019

@author: sceaj
'''

from datetime import timedelta

class DataState(object):
    '''
    classdocs
    '''

    def state_time = timedelta(days = 0, seconds = 0, milliseconds = 0)
    def last_state_time = timedelta(days = 0, seconds = 0, milliseconds = 0)
    def state = dict()

    def __init__(self, params):
        '''
        Constructor
        '''
        
    def set_data_time(self, time):
        state_time = time
        if state_time > last_state_time:
            callback
        
    def set_data_item(self, key, value):